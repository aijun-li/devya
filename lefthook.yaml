pre-commit:
  parallel: true
  commands:
    'lint frontend':
      run: npx eslint --fix {staged_files}
    'lint backend':
      root: src-tauri
      run: cargo fmt --check && cargo check

commit-msg:
  commands:
    'lint commit message':
      run: npx commitlint --edit {1}
